{
  "name": "incident-reports",
  "version": "1.0.0",
  "description": "Incident Reports App with Netlify Functions",
  "dependencies": {
    "cors": "^2.8.5",
    "mongodb": "^7.0.0",
    "mongoose": "^8.16.3"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && npm run dev"
  }
}
